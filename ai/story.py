# ai/story.py
# STUB — Person 4 replaces this with real Gemini/LLaMA narration script.
# Contract: same function signature must be kept.

def generate_narration_script(captions: list[str], language: str, model: str) -> str:
    """
    Input:  captions (list of English strings), language code, model name
    Output: single narration script string
    """
    # --- STUB: joins captions into a basic script ---
    # Person 4: replace this body with real Gemini API call
    joined = " ".join(captions)
    return f"Welcome to this story. {joined} Thank you for watching."

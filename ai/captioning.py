# ai/captioning.py
# STUB — Person 3 replaces this with real BLIP implementation.
# Contract: same function signature must be kept.

def generate_captions(image_paths: list[str]) -> list[str]:
    """
    Input:  list of absolute image file paths
    Output: list of English caption strings (same length and order as input)
    """
    # --- STUB: returns placeholder captions ---
    # Person 3: replace this body with real BLIP inference
    return [f"A photo showing scene {i+1}." for i in range(len(image_paths))]
